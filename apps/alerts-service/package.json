{
  "name": "alerts-service",
  "version": "0.0.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@repo/mqtt-client": "workspace:*",
    "@repo/types": "workspace:*",
    "mqtt": "^5.15.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
